name: Prepare Release

on:
  workflow_dispatch: 
  pull_request:
    paths:
       - .github/workflows/prepare-release.yml   
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Base workflow registered."
